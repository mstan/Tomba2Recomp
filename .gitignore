# Build outputs
/build/
/build-*/
/out/
/.happypath/

# Recompiler output (derivative of copyrighted game code — regenerate locally)
/generated/

# Release packaging artifacts
/release-stage/
*.zip

# Local PlayStation assets (disc image, extracted boot EXE, memory cards)
/tomba2/
/isos/
/saves/
/_extract/

# Disc / image / memcard formats
*.bin
*.cue
*.iso
*.img
*.sub
*.ccd
*.mcd
*.mcr
*.chd

# Ghidra: keep instructions.txt + scripts/ + annotations/, drop the live DB + dumps
/ghidra/projects/
/ghidra/*_no_header.bin
*.gpr
*.rep/
*.lock
*.lock~

# Local editor / cache files
.vs/
.vscode/
*.user
*.log

# Runtime artifacts from psx-runtime launches
psx_crash.txt
psx_last_run_report.json
psx_screenshot.bmp
psx_freeze_heartbeat.json
starvation_dump.jsonl
/overlay_captures.addendum.jsonl
/.aot_capture_history/

# Diagnostic dumps from debug-server probes (root-level only; keep annotations/ tracked)
/*.json
/*.bmp
/*.png

# Python caches
__pycache__/

# Regen backup dirs (regen-time safety copies of generated/)
/generated.*/

# Session debug logs / probe scratch at repo root (agent + soak runs)
/_*.txt
/_*.err
/_*.log

# Release packaging: staged tree, zip, and the downloaded toolchain cache
/release-stage/
*.zip
/tools/_toolchain_cache/
